# SithafalTask2
This script enables querying a website's content by scraping multiple URLs, embedding the text into a FAISS index, and retrieving relevant information based on user queries. It uses the Gemini API for authentication and employs the SentenceTransformer model for text embeddings.
